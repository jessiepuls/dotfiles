#!/bin/sh

GREEN='\033[0;32m'
YELLOW='\033[0;33m'
RESET='\033[0m'

declare -a to_install=(
    "golang.go"
    "eamodio.gitlens"
    "iliazeus.vscode-ansi"
    "hashicorp.terraform"
    "HashiCorp.HCL"
    "ms-azuretools.vscode-docker"
    "ms-python.python"
    "ms-vscode-remote.remote-containers"
    "ms-vscode-remote.remote-ssh"
    "rebornix.Ruby"
    "vscjava.vscode-java-pack"
    "WakaTime.vscode-wakatime"
)

declare -a installed=`code --list-extensions`

echo "\n💻 Installing VSCode Extensions\n"
for i in "${to_install[@]}"
do
    if [[ ! ${installed[*]} =~ ${i} ]]; then
        printf "${GREEN}Installing ${i}${RESET}\n"
        code --install-extension $i
    fi

    if [[ ${installed[*]} =~ ${i} ]]; then
        printf "${YELLOW}${i} previously installed${RESET}\n"
    fi
done

echo "\n🦄 Creating code directory structure\n"

# Make source code directories
echo "\n🗂  Creating code directory structure\n"
mkdir -p ~/code/td
mkdir -p ~/code/personal
mkdir -p ~/code/mode